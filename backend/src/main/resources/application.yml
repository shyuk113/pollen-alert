spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/pollen_alert
    username: postgres
    password: 1478
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8080